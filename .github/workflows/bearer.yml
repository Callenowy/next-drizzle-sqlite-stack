name: Bearer

on:
  push:
    branches:
      - main
      - dev

permissions:
  contents: read

jobs:
  bearer:
    name: Bearer
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Report
        id: report
        uses: bearer/bearer-action@v2